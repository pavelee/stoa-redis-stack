import { FunctionComponent } from "react"

export const PointCounter: FunctionComponent = ({ }) => {
    return (
        <div className="w-12 h-12 bg-[#ffd700] rounded-full border-4 border-[#ccad00] flex justify-center items-center font-bold">
            0
        </div>
    )
}